[project]
name = "mcp-memory"
version = "0.1.0"
description = "MCP server for persistent markdown memory storage"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.26",
    "python-dotenv",
    "httpx>=0.27",
]
